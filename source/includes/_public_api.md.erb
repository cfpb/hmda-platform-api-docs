# HMDA Public API

The following are HMDA Public API end points


### Health Check

```shell
curl "<%= config[:publicapihost] %>"
```

Health checks could be preformed to see if the api server is live

<br><br>

```json
{
    "status":"OK",
    "service":"hmda-public-api",
    "time":"2019-01-01T01:01:51.866Z",
    "host":"HOSTNAME"
}
```
